server:
  port: 8085

spring:
  application:
    name: audit-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/auditdb

management:
  endpoints:
    web:
      exposure:
        include: "*"